import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "via.placeholder.com",
      "images.unsplash.com",
      "source.unsplash.com",
      "example.com",
      "i.ibb.co",
    ],
  },
};

export default nextConfig;
